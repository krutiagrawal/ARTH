import ContactClient from './ContactClient'

export const metadata = {
  title: 'Contact',
  description: 'Write to ARTH – questions, partnerships, press. We usually respond within a few days.',
}

export default function Page() {
  return <ContactClient />
}
