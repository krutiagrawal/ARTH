import MissionClient from './MissionClient'

export const metadata = {
  title: 'Mission',
  description: 'ARTH exists to plant a forest we will not live to see fully grown — our statement of intent.',
}

export default function Page() {
  return <MissionClient />
}
