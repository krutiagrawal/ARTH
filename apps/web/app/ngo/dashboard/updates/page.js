import UpdatesClient from './UpdatesClient'

export const metadata = { title: 'Updates · NGO dashboard', robots: { index: false, follow: false } }

export default function Page() {
  return <UpdatesClient />
}
