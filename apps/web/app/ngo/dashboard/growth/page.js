import GrowthClient from './GrowthClient'

export const metadata = {
  title: 'Growth & Trust · NGO dashboard',
  robots: { index: false, follow: false },
}

export default function Page() {
  return <GrowthClient />
}
