import SurvivalClient from './SurvivalClient'

export const metadata = { title: 'Survival & Impact · NGO dashboard', robots: { index: false, follow: false } }

export default function Page() {
  return <SurvivalClient />
}
