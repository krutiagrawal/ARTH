import GroupLoginClient from './GroupLoginClient'

export const metadata = {
  title: 'Group sign in',
  robots: { index: false, follow: false },
}

export default function Page() {
  return <GroupLoginClient />
}
