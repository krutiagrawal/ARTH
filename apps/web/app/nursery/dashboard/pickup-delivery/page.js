import PickupDeliveryClient from './PickupDeliveryClient'

export const metadata = {
  title: 'Pickup & Delivery · Nursery dashboard',
  robots: { index: false, follow: false },
}

export default function Page() {
  return <PickupDeliveryClient />
}
