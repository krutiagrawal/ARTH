// Mirrors the server's ForbiddenError message in
// services/api/src/services/plantingLocation.service.ts — kept in one place so the
// client-side popup and the server-enforced rejection always show the same copy.
export const NOT_APPROVED_MESSAGE =
  'This place is not approved for planting, check out ARTH approved places.';
